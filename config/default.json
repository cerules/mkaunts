{
    "host": "localhost",
    "port": 3000,
    //todo: store all values below this comment in a local config and don't use any of these values because they are insecure
    "MongoConnectionString": "mongodb://localhost/mkaunts", //"mongodb://admin:password@localhost/mkaunts", 
    "jwtSecret": "secret",
    "sslKeyPath": "sslKeys/mkaunts-key.pem",
    "sslCertPath": "sslKeys/mkaunts-cert.pem"
}